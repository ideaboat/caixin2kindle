# caixin2kindle — 《财新周刊》下载并推送 Kindle 工具 — 规格文档

## 1. 概述

### 1.1 目的

用户输入《财新周刊》某一期的网页地址，程序自动完成：

1. 打开浏览器访问该期页面，解析当期所有文章列表
2. 逐篇获取全文（模拟点击"余下全文"或"下一页"展开/加载全文）
3. 纯文字整理成 EPUB 电子书
4. 通过 calibre 转换为 MOBI（兼容老款 Kindle）
5. 自动检测 USB 连接的 Kindle 设备，复制文件

### 1.2 目标用户场景

> 先生打开财新网页版，找到某期杂志的链接 → 粘贴给程序 → 等几分钟 → Kindle 里多了一期杂志的若干文章。

### 1.3 约束与前提

| 项 | 值 |
|---|---|
| 语言 | Go |
| 运行平台 | macOS |
| 归档格式 | EPUB |
| Kindle 格式 | MOBI（calibre 转换，目标设备为老款 Kindle，仅支持 MOBI） |
| 格式转换 | calibre 命令行工具 `ebook-convert`（已安装） |
| Kindle 连接 | USB 线直连，挂载于 `/Volumes/Kindle`（可配置） |
| 浏览器自动化 | chromedp（Go），**默认有头模式** |
| 认证方式 | 持久化浏览器 profile + 首次手动登录 |
| 正文内容 | 纯文字（不含图片） |

### 1.4 合规边界

仅用于个人已付费订阅内容的离线阅读，单账号、低频访问。不做批量抓取、不分发内容。

---

## 2. 命令行接口

最常用（也是唯一必填的）用法：

```
caixin2kindle https://weekly.caixin.com/2026/cw1224/
```

URL 为唯一位置参数，指向某期周刊页面。

可选参数：

| 参数 | 说明 | 默认值 |
|---|---|---|
| `--out <dir>` | 输出目录 | `~/downloads/caixin/<期号>/` |
| `--no-kindle` | 只产出 EPUB + MOBI，不拷贝到 Kindle | 关闭 |
| `--full` | 全量重跑：忽略已有进度，重新抓取全部文章并重建 EPUB/MOBI | 关闭（缺省为增量） |
| `--headless` | 无头模式运行浏览器（风控风险更高，无法手动处理验证码） | 关闭（即有头） |
| `--delay "3-8"` | 相邻两篇文章抓取之间的等待秒数区间，每次随机取区间内一值（如 `3-8` 表示 3~8 秒随机等待，避免固定间隔被风控识别） | `3-8` |

设计原则：不加配置文件，参数保持最少；以上默认值对主场景开箱即用。

---

## 3. 核心流程

```
caixin2kindle <url>
  1. 启动 Chrome（有头，持久化 profile）
  2. 访问 issue 页 → 解析期号、文章列表
  3. 逐篇访问文章页 → 模拟点击展开全文 → 提取正文
  4. 组装 EPUB（纯文字）
  5. ebook-convert → MOBI
  6. 检测 Kindle 挂载 → 复制 MOBI → 完成
```

### 3.1 认证（持久化 profile + 首次手动登录）

- 使用固定 user-data-dir（默认 `~/.caixin/browser-profile`），cookie/登录态跨运行复用。
- 首次运行或登录态失效时：程序打开浏览器停在财新登录页，提示用户手动登录；检测到登录成功后自动继续。
- 运行中若遇验证码/风控弹窗：暂停等待用户在有头窗口中手动处理，再继续。

### 3.2 浏览器模式

- **默认有头**：财新反爬会检测 headless 特征，有头模式通过率高，且登录、验证码均可人工介入。
- `--headless` 仅作为可选加速手段保留。
- 运行期间用户不应操作自动化窗口（spec 与 `--help` 中注明）。

### 3.3 反爬与拟人化（硬性要求）

- 每篇文章之间随机间隔 3–8 秒（`--delay` 可调）。
- 点击"余下全文"/"下一页"前：滚动页面、等待元素可见；点击后等待网络空闲或目标元素就绪，而非固定 sleep。
- 所有翻页/展开动作通过**模拟真实点击**完成，不直接调用内部 API。
- 单次运行默认抓取当期全部文章（一期 30+ 篇约 3–5 分钟，属预期）。

### 3.4 文章全文获取

财新存在两种全文机制，两种都要支持：

1. 点击"余下全文"按钮就地展开；
2. 分页式"下一页"持续加载。

正文提取要求：提取标题、作者、正文段落纯文本；不抓取图片、广告、推荐位。

### 3.5 EPUB 组装

- 目录名：从 issue 页解析期号（如"财新周刊第1234期"），解析失败回退用 URL slug（如 `2026-cw1224`）。
- 输出结构：

```
~/downloads/caixin/财新周刊第1234期/
  └── 财新周刊第1234期.epub
```

- EPUB 元数据：书名 = 期号，文章按当期目录顺序作为章节，含作者署名。纯文字，无图片。

### 3.6 calibre 转换与 Kindle 拷贝

```
ebook-convert <期号>.epub <期号>.mobi --output-profile kindle
```

- 转换成功后才检测 Kindle——**程序启动时不做设备检测**，检测仅发生在需要拷贝这一步（未指定 `--no-kindle` 且 MOBI 已生成时）。扫描 `/Volumes/`，典型为 `/Volumes/Kindle`。
- 检测到 Kindle：将 MOBI 复制到其 `documents/` 目录。
- 未检测到 Kindle：提示"未检测到设备，成品文件位于 <输出目录路径>，请自行拷入"，**正常退出，不算失败**。
- `--no-kindle` 时跳过检测与拷贝。

---

## 4. 可靠性

- **幂等/增量（缺省行为）**：重复运行同一期时，已有成功抓取的文章跳过，只补缺失的；EPUB/MOBI 存在且文章数一致时跳过重建。
- **`--full` 全量重跑**：当用户发现内容有问题需要彻底重抓时使用——忽略既有进度，重新抓取当期全部文章并重建 EPUB/MOBI。
- **失败退避**：单篇失败重试 1–2 次，间隔指数递增；连续失败则停止运行，提示用户检查登录态，不做高频硬刷。
- **登录态失效**：中途检测到被登出时暂停，等待用户重新登录后继续。

---

## 5. 非目标（v1 不做）

- 图片抓取与嵌入
- 配置文件
- 多期批量、增量订阅
- Send-to-Kindle 云端推送
- Windows/Linux 支持
- 直接调用财新内部 API 抓取

---

## 6. 待验证事项（实现阶段确认）

- [ ] issue 页文章列表与期号的 selector
- [ ] "余下全文"与"下一页"两种机制的页面行为细节
- [ ] 老款 Kindle 具体型号对 MOBI 子格式（KF7/KF8）的兼容性，必要时调整 `--output-profile`
- [ ] 首次登录检测的完成信号（URL 跳转或页面元素）
