# caixin2kindle — 《财新周刊》下载并推送 Kindle 工具 — 规格文档

## 1. 概述

### 1.1 目的

用户输入《财新周刊》某一期的网页地址，程序自动完成：

1. 打开浏览器访问该期页面，解析当期所有文章列表
2. 逐篇获取全文（模拟点击"余下全文"或"下一页"展开/加载全文）
3. 纯文字整理成 EPUB 电子书
4. 通过 calibre 转换为 MOBI（兼容老款 Kindle）
5. 检测 USB 连接的 Kindle 设备，复制文件

### 1.2 目标用户场景

> 先生打开财新网页版，找到某期杂志的链接 → 粘贴给程序 → 等几分钟 → Kindle 里多了一期杂志的若干文章。

### 1.3 约束与前提

| 项 | 值 |
|---|---|
| 语言 | Go |
| 运行平台 | macOS |
| 归档格式 | EPUB 3 |
| Kindle 格式 | MOBI（calibre 转换，目标设备为老款 Kindle，仅支持 MOBI；具体型号及 KF7/KF8 兼容性待确认，见第 6 章） |
| 格式转换 | calibre 命令行工具 `ebook-convert`（已安装） |
| Kindle 连接 | USB 线直连；挂载点由 `--kindle` 指定，默认 `/Volumes/Kindle` |
| 浏览器自动化 | chromedp（Go），**默认有头模式** |
| 认证方式 | 持久化浏览器 profile + 首次手动登录 |
| 正文内容 | 纯文字（不含图片） |
| 并发模型 | 单线程，按当期目录顺序逐篇抓取，不并发 |

### 1.4 合规边界

仅用于个人已付费订阅内容的离线阅读，单账号、低频访问。不做批量抓取、不分发内容。

---

## 2. 命令行接口

最常用（也是唯一必填的）用法：

```
caixin2kindle https://weekly.caixin.com/2026/cw1224/
```

URL 为唯一位置参数，指向某期周刊页面。

可选参数：

| 参数 | 说明 | 默认值 |
|---|---|---|
| `--out <dir>` | **父目录**：最终输出到 `<out>/<期号>/` | `~/Downloads/caixin` |
| `--no-kindle` | 只产出 EPUB + MOBI，不拷贝到 Kindle | 关闭 |
| `--full` | 全量重跑：忽略已有进度，重新抓取全部文章并重建 EPUB/MOBI | 关闭（缺省为增量） |
| `--kindle <mount>` | Kindle 挂载点 | `/Volumes/Kindle` |
| `--headless` | 无头模式运行浏览器（风控风险更高，无法手动处理验证码） | 关闭（即有头） |
| `--delay "3-8"` | 相邻两篇文章抓取之间的等待秒数区间，每次随机取区间内一值（如 `3-8` 表示 3~8 秒随机等待，避免固定间隔被风控识别） | `3-8` |

设计原则：不加配置文件、不加环境变量，参数保持最少；以上默认值对主场景开箱即用。

---

## 3. 核心流程

```
caixin2kindle <url>
  0. 依赖检查（Chrome、ebook-convert）
  1. 启动 Chrome（有头，持久化 profile）
  2. 访问 issue 页 → 滚动加载至列表稳定（终止条件见 3.6）→ 解析期号、文章列表
  3. 逐篇访问文章页 → 循环点击展开全文 → 提取正文
  4. 组装 EPUB（纯文字）
  5. ebook-convert → MOBI
  6. 检测 Kindle 挂载 → 复制 MOBI → 完成
```

### 3.1 依赖检查与启动

- 启动时检查：Chrome 是否安装、`ebook-convert` 是否在 PATH 中；缺失时输出对应安装提示并退出（退出码 1）。
- 浏览器 profile 目录固定为 `~/.caixin/browser-profile`，权限设为 `0700`（内含登录态）；启动时若检测到 profile 被占用（Chrome SingletonLock，已有实例在运行），提示后以退出码 2 退出。
- 日志默认输出到 **stderr**；进度日志格式见 4.5。日志永不输出 cookie 及请求头中的凭据。

### 3.2 认证（持久化 profile + 首次手动登录）

登录态失效与人工介入按以下三层信号处理：

1. **登录检测**：访问 issue 页后，若跳转至登录页或出现登录表单 → 终端提示"请在浏览器中手动登录"，轮询等待文章列表元素出现（设超时）后继续。
2. **验证码/风控**：终端提示"请在浏览器窗口完成验证后按回车继续"，用户确认后继续。人工确认优于不可靠的自动检测。
3. **超时兜底**：所有等待（登录、元素、网络空闲）必须设超时上限，超时按失败处理，杜绝无限卡死。

### 3.3 浏览器模式

- **默认有头**：财新反爬会检测 headless 特征，有头模式通过率高，且登录、验证码均可人工介入。
- `--headless` 仅作为可选加速手段保留。
- 运行期间用户不应操作自动化窗口（spec 与 `--help` 中注明）。

### 3.4 反爬与拟人化（硬性要求）

- 相邻两篇文章之间随机等待 `--delay` 区间内秒数（默认 3–8 秒）。
- 同一篇文章内的多次"余下全文"/"下一页"点击之间，加 1–3 秒随机等待。
- 点击前：滚动页面、等待元素可见；点击后等待网络空闲或目标元素就绪，而非固定 sleep。
- 所有翻页/展开动作通过**模拟真实点击**完成，不直接调用内部 API。
- 单次运行默认抓取当期全部文章（一期 30+ 篇约 3–5 分钟，属预期）。

### 3.5 页面锚点策略（selector 原则）

实现阶段无法靠猜测定 selector，遵循以下策略：

1. 优先选取**语义化、结构稳定**的锚点（如具有稳定 id/class/文本特征的按钮与容器），避免依赖深层嵌套的脆弱路径。
2. 全部 selector 集中定义在代码中**唯一一处**（如一个 selectors 常量文件），页面改版时只需修一处。
3. 实现第一步是**收集三类样例页面并保存 HTML 快照**至 `testdata/`，作为 selector 开发与回归测试的 fixture：
   - issue 页 1 个（真实期号 URL）；
   - "余下全文"机制文章 1 篇；
   - "下一页"机制文章 1 篇。
4. 样例 URL 与快照属于实现阶段产物（`testdata/`），不入本需求文档（含付费内容，且有合规顾虑）。

### 3.6 文章列表解析

- issue 页可能存在懒加载：解析前**滚动至页面底部并重复加载，直至文章列表数量连续两次滚动后不再增加**（列表稳定），再解析。
- 解析期号与文章列表（标题、URL、目录顺序）。
- **去重**：按文章 URL 归一化（去尾部 `/` 与 query）去重，重复链接（如封面文章在列表出现两次）算一篇，保留首次出现位置；后续所有计数与验收均基于去重后列表。
- **期号解析**：匹配"第 \d+ 期"类模式，容忍"财新周刊第1234期""财新周刊 | 第1234期"等文案变体；真实文案格式依 `testdata/` 快照确认（见第 6 章）。

### 3.7 文章全文获取

财新存在两种全文机制，**两种可能并存**。对每篇文章：先探测页面包含哪种机制，再对存在的机制分别循环处理：

1. 若存在"余下全文"：循环点击，直至按钮消失；
2. 若存在"下一页"：循环点击，直至没有下一页；
3. 正文按出现顺序拼接；标题与作者只提取一次；
4. 剔除页脚、推荐位、广告等非正文元素；
5. **单篇点击上限 50 次**（两种机制合计），超过视为异常、按失败处理（防死循环）；
6. **成功判定**：提取的正文以财新文末特征符号收尾（具体符号形态依 `testdata/` 快照确认，见第 6 章）。未检出该符号，或出现付费提示/未展开预览文案，视为该篇失败，走 4.3 重试流程。不以字数阈值判定。

正文提取要求：标题、作者、正文段落纯文本；不抓取图片。

### 3.8 EPUB 组装

- 目录名：从 issue 页解析期号（如"财新周刊第1234期"）；解析失败回退 URL slug，规则为取 URL 中的年/期路径段拼连，如 `weekly.caixin.com/2026/cw1224/` → `2026-cw1224`。
- 期号/目录名做 sanitize：替换 `/`、`:` 等文件系统非法字符。
- EPUB 元数据：EPUB 3，语言 `zh-CN`，书名 = 期号，作者 = 财新周刊，章节按当期目录顺序、每章含作者署名。纯文字，无图片。
- 输出结构（`--out` 为父目录）：

```
<out>/财新周刊第1234期/
  ├── 财新周刊第1234期.epub
  ├── 财新周刊第1234期.mobi
  ├── articles/           # 中间产物：每篇纯文本
  └── .caixin2kindle/
      └── state.json      # 抓取状态
```

### 3.9 calibre 转换与 Kindle 拷贝

```
ebook-convert <期号>.epub <期号>.mobi --output-profile kindle
```

- 转换成功后才检测 Kindle——**程序启动时不做设备检测**，检测仅发生在需要拷贝这一步（未指定 `--no-kindle` 且 MOBI 已生成时）。检测规则：
  1. `--kindle` 指定的挂载点存在 → 使用之；
  2. 否则扫描 `/Volumes/*`，取第一个含 `documents/` 目录的卷；
  3. 均无 → 未检测到。
- 检测到 Kindle：将 MOBI 复制到其 `documents/` 目录；**同名文件已存在则覆盖**（与 `--full` 重建语义一致）；复制失败明确报错、退出码非 0。
- 未检测到 Kindle：提示"未检测到设备，成品文件位于 <输出目录>，请自行拷入"，**正常退出（退出码 0），不算失败**。
- `--no-kindle` 时跳过检测与拷贝。

---

## 4. 可靠性

### 4.1 增量（缺省行为）与状态文件

以 `<输出目录>/.caixin2kindle/state.json` 为唯一进度依据，**不靠数文章数**：

- `state.json` 记录期号，以及每篇文章的 URL、标题、状态（success/fail/pending）、纯文本文件路径、内容哈希。
- **内容哈希对提取后的正文纯文本计算**，不对原始 HTML（原始 HTML 含时间戳、推荐位等动态注入内容，会导致哈希误变、增量失效）。
- 重跑时：状态为 success 且内容哈希未变的文章跳过，其余补抓。
- **期号隔离**：输出子目录按期号命名，同一 `--out` 先后运行不同期各自生成独立子目录与 state，互不干扰，无需冲突处理。
- EPUB/MOBI 重建判定：state 中全部文章 success **且**期号未变 → 跳过重建；否则重建。

### 4.2 `--full` 全量重跑

用户发现内容有问题需要彻底重抓时使用：忽略/重置既有 state，重新抓取当期全部文章并重建 EPUB/MOBI。

### 4.3 失败重试

- 单篇文章失败最多重试 2 次，间隔 2 秒、4 秒（固定退避）。
- **连续 3 篇**文章失败即停止运行，报错附排查提示：登录态是否失效 / 网络是否正常 / 页面结构是否变更。

### 4.4 登录态失效（中途）

运行中检测到被登出时暂停，按 3.2 的信号机制等待用户重新登录后继续。

### 4.5 退出码与日志

| 退出码 | 含义 |
|---|---|
| 0 | 成功（含未检测到 Kindle 而提示自行拷入的情形） |
| 1 | 依赖缺失（Chrome / ebook-convert）或浏览器启动失败（含 profile 被占用） |
| 2 | 登录失败或登录超时 |
| 3 | 抓取失败（含连续 3 篇失败停止、单篇点击超限） |
| 4 | EPUB/MOBI 转换失败 |
| 5 | Kindle 复制失败 |

- 进度日志逐篇输出，格式：`第 3/32 篇：<标题>`。

---

## 5. 非目标（v1 不做）

- 图片抓取与嵌入
- 配置文件、环境变量、日志级别开关
- 多期批量、增量订阅
- Send-to-Kindle 云端推送
- Windows/Linux 支持
- 直接调用财新内部 API 抓取

---

## 6. 待验证事项（实现阶段确认，第一项为开发前置）

- [ ] **收集三类样例页面 HTML 快照至 `testdata/`**（issue 页 / "余下全文"文章 / "下一页"文章），作为 selector 开发前置
- [ ] 依快照确定列表、标题、作者、正文、按钮的具体锚点（写入集中 selector 定义）
- [ ] 依快照确认：文末特征符号的具体形态、付费提示/预览文案特征、期号文案的真实格式
- [ ] 老款 Kindle 具体型号对 MOBI 子格式（KF7/KF8）的兼容性，必要时调整 `--output-profile`

---

## 7. 验收标准

1. 抓取文章数 = issue 页列表去重后的文章数；
2. 每篇正文完整（以文末特征符号收尾）且无付费提示残留；
3. EPUB 可正常打开（calibre 导入验证），元数据与章节正确；
4. `ebook-convert` 成功产出 MOBI；
5. 检测到 Kindle 时拷贝成功；未检测到时输出路径提示且退出码为 0；
6. 重跑同一期：已成功文章不重复抓取（增量生效）；
7. `--full` 后文章全部重新抓取并重建 EPUB/MOBI。
