# caixin2kindle — 《财新周刊》下载并推送 Kindle 工具 — 规格文档

## 1. 概述

### 1.1 目的

用户输入《财新周刊》某一期的网页地址，程序自动完成：

1. 打开浏览器访问该期页面，解析当期所有文章列表
2. 逐篇获取全文（模拟点击"余下全文"或"下一页"展开/加载全文）
3. 纯文字整理成 EPUB 电子书
4. 通过 calibre 转换为 MOBI（兼容老款 Kindle）
5. 检测 USB 连接的 Kindle 设备，复制文件

### 1.2 目标用户场景

> 先生打开财新网页版，找到某期杂志的链接 → 粘贴给程序 → 等几分钟 → Kindle 里多了一期杂志的若干文章。

### 1.3 约束与前提

| 项 | 值 |
|---|---|
| 语言 | Go |
| 运行平台 | macOS |
| 归档格式 | EPUB 3 |
| Kindle 格式 | MOBI（calibre 转换，目标设备为老款 Kindle，仅支持 MOBI；具体型号及 KF7/KF8 兼容性待确认，见第 6 章） |
| 格式转换 | calibre 命令行工具 `ebook-convert`（已安装） |
| Kindle 连接 | USB 线直连；挂载点由 `--kindle` 指定，默认 `/Volumes/Kindle` |
| 浏览器自动化 | chromedp（Go），**默认有头模式**；支持 Chrome / Chrome Canary / Chromium / Brave / Edge，可用 `--browser` 显式指定 |
| 认证方式 | 持久化浏览器 profile + 首次手动登录 |
| 正文内容 | 纯文字（不含图片） |
| 并发模型 | 单线程，按当期目录顺序逐篇抓取，不并发 |

### 1.4 合规边界

仅用于个人已付费订阅内容的离线阅读，单账号、低频访问。不做批量抓取、不分发内容。

---

## 2. 命令行接口

最常用（也是唯一必填的）用法：

```
caixin2kindle https://weekly.caixin.com/2026/cw1224/
```

URL 为唯一位置参数，指向某期周刊页面。

可选参数：

| 参数 | 说明 | 默认值 |
|---|---|---|
| `--out <dir>` | **父目录**：最终输出到 `<out>/<期号>/` | `~/Downloads/caixin` |
| `--no-kindle` | 只产出 EPUB + MOBI，不拷贝到 Kindle | 关闭 |
| `--full` | 全量重跑：忽略已有进度，重新抓取全部文章并重建 EPUB/MOBI | 关闭（缺省为增量） |
| `--kindle <mount>` | Kindle 挂载点 | `/Volumes/Kindle` |
| `--headless` | 无头模式运行浏览器（风控风险更高，无法手动处理验证码） | 关闭（即有头） |
| `--browser <path>` | 指定浏览器可执行文件路径；显式指定但路径不存在（或是目录）时**报错退出，不回退到自动探测** | 自动探测 |
| `--delay "3-8"` | 相邻两篇文章抓取之间的等待秒数区间，每次随机取区间内一值（如 `3-8` 表示 3~8 秒随机等待，避免固定间隔被风控识别） | `3-8` |

设计原则：不加配置文件、不加环境变量，参数保持最少；以上默认值对主场景开箱即用。

---

## 3. 核心流程

```
caixin2kindle <url>
  0. 依赖检查（Chrome、ebook-convert）
  1. 启动 Chrome（有头，持久化 profile）
  2. 访问 issue 页 → 滚动加载至列表稳定（终止条件见 3.6）→ 解析期号、文章列表
  3. 逐篇访问文章页 → 循环点击展开全文 → 提取正文
  4. 组装 EPUB（纯文字）
  5. ebook-convert → MOBI
  6. 检测 Kindle 挂载 → 复制 MOBI → 完成
```

### 3.1 依赖检查与启动

- 启动时检查：**Chromium 系浏览器**是否可用、`ebook-convert` 是否在 PATH 中；缺失时输出对应安装提示并退出（退出码 1）。浏览器三级定位：① 显式 `--browser`（不存在或是目录 → 报错退出 1，**不回退**）→ ② 自动探测取第一个存在者：Chrome → Chrome Canary → Chromium → Brave → Edge → ③ PATH 兜底：`google-chrome` → `chromium` → `brave-browser`。
- 浏览器 profile 目录固定为 `~/.caixin/browser-profile`，权限设为 `0700`（内含登录态）；启动时若检测到 profile 被占用（Chrome SingletonLock，已有实例在运行），提示后以退出码 1 退出。
- 日志默认输出到 **stderr**；进度日志格式见 4.5。日志永不输出 cookie 及请求头中的凭据。

### 3.2 认证（持久化 profile + 首次手动登录）

登录态失效与人工介入按以下三层信号处理：

1. **权限检测落在文章页，不落在列表页（2026-09-25 实测定为事实）**：期号页**不设登录门槛**，未登录也能拿到完整文章列表，因此"访问 issue 页后检测登录表单"在本站不成立。未登录/无权限的唯一差别出现在文章页：正文只剩约 400 字预览，且 `div#chargeWallContent` 可见。故：访问**文章页**后若该层可见（或页面跳到登录页、出现登录表单）→ 终端提示"请在浏览器中手动登录"，等待用户完成（人工确认本身无超时，见第 3 条与架构 §9.2）后重试该篇。
2. **验证码/风控**：终端提示"请在浏览器窗口完成验证后按回车继续"，用户确认后继续。人工确认优于不可靠的自动检测。
3. **超时兜底**：所有**机器等待**（元素、网络空闲、导航）必须设超时上限，超时按失败处理，杜绝无限卡死；只有"等待用户按回车"这一处不设超时（架构 §9.2）。

### 3.3 浏览器模式

- **默认有头**：财新反爬会检测 headless 特征，有头模式通过率高，且登录、验证码均可人工介入。
- `--headless` 仅作为可选加速手段保留。
- 运行期间用户不应操作自动化窗口（spec 与 `--help` 中注明）。

### 3.4 反爬与拟人化（硬性要求）

- 相邻两篇文章之间随机等待 `--delay` 区间内秒数（默认 3–8 秒）。
- 同一篇文章内的多次"余下全文"/"下一页"点击之间，加 1–3 秒随机等待。
- 点击前：滚动页面、等待元素可见；点击后等待网络空闲或目标元素就绪，而非固定 sleep。
- 所有翻页/展开动作通过**模拟真实点击**完成，不直接调用内部 API；真实点击失败时允许降级为 DOM 级 `el.click()` 并留日志（仍属 DOM 操作，非内部 API）。
- **抑制自动化指纹**：启动参数固定关闭 `--enable-automation`、关闭 Blink 的 `AutomationControlled` 运行时特性（只要挂了 CDP，浏览器就会打开该特性，**只关前一项不足以让 `navigator.webdriver` 变 false**，等于向站点自报自动化），并用 `--test-type` 抑制随之出现的「不受支持的命令行标记」提示条；启动后核对一次 `navigator.webdriver === false`。
- 单次运行默认抓取当期全部文章（一期 30+ 篇约 3–5 分钟，属预期）。

### 3.5 页面锚点策略（selector 原则）

实现阶段无法靠猜测定 selector，遵循以下策略：

1. 优先选取**语义化、结构稳定**的锚点（如具有稳定 id/class/文本特征的按钮与容器），避免依赖深层嵌套的脆弱路径。
2. 全部 selector 集中定义在代码中**唯一一处**（如一个 selectors 常量文件），页面改版时只需修一处。
3. 实现第一步是**收集三类样例页面并保存 HTML 快照**至 `testdata/`，作为 selector 开发与回归测试的 fixture：
   - issue 页 1 个（真实期号 URL）；
   - "余下全文"机制文章 1 篇；
   - "下一页"机制文章 1 篇。
4. 样例 URL 与快照属于实现阶段产物（`testdata/`），不入本需求文档（含付费内容，且有合规顾虑）。

### 3.6 文章列表解析

- issue 页可能存在懒加载：解析前**滚动至页面底部并重复加载，直至文章列表数量连续两次滚动后不再增加**（列表稳定），再解析。
- 解析期号与文章列表（标题、URL、目录顺序）。
- **去重**：按文章 URL 归一化（去尾部 `/` 与 query）去重，重复链接（如封面文章在列表出现两次）算一篇，保留首次出现位置；后续所有计数与验收均基于去重后列表。
- **期号解析**：匹配"第 \d+ 期"类模式，容忍"财新周刊第1234期""财新周刊 | 第1234期"等文案变体；真实文案格式依 `testdata/` 快照确认（见第 6 章）。

### 3.7 文章全文获取

财新存在两种全文机制，**两种可能并存**。对每篇文章：先探测页面包含哪种机制，再对存在的机制分别循环处理：

1. 若存在"余下全文"：**单击一次即展开整篇正文**。点击后若正文已完整（见第 6 条判定），**不再继续点击**；仅当仍未完整且按钮仍在时，才继续点击，直至按钮消失；
2. 若存在"下一页"：循环点击，直至没有下一页（选择翻页路线时须一直点到没有下一页，才能读完全文）；
3. 两种机制并存时，优先处理"余下全文"（展开后若正文已完整则无需翻页）；展开后仍未完整且存在"下一页"时，再依次翻页处理；
4. 正文按出现顺序拼接；标题与作者只提取一次；
5. 剔除页脚、推荐位、广告等非正文元素；
6. **单篇点击上限 50 次**（"余下全文"与"下一页"合计），超过视为异常、按失败处理（防死循环）；
7. **成功判定**（依 `testdata/` 快照定稿，2026-09-25）：财新页面**不存在**符号型文末特征（实测无 ■/◆/▲ 等收尾符号；`div.lanmu_textend`、`div.idetor` 每个分页都会出现，不能作完整性判据），改用三条可验证的 DOM 事实：
   - **无付费墙**：`div#chargeWallContent` 不可见（登录态为 inline `display:none`；未登录时该层可见，且正文只剩约 400 字预览）；
   - **按钮穷尽**：展开路线 `div#pageBtn` 已空；翻页路线 `#pageBtn` 内不再有「下一页」；
   - **小节齐全**：正文中出现的 `h2.cx-app-content-subheads` 覆盖 `ul#pageNav li` 的全部小节标题（去序号前缀后比对）——即"没漏页"的正面校验；页面无 `#pageNav` 时该条自然成立。
   - 判定须在**整篇正文拼接完成后**再执行一次；任一条件不满足即判该篇失败，走 4.3 重试流程，不得写为成功状态。不以字数阈值判定。
8. **图片型栏目直接跳过**（2026-09-25 需求方确认）：标题以「显影」开头的报道以图片为主、正文容器与文字稿不同，纯文字 EPUB 无法承载其内容，故**不抓取**该篇；状态记为 `skipped`（既不算成功也不算失败），并在输出中给出「已跳过（N 篇）：…」与原因说明。该判定只看期号页列表标题前缀，不依赖文章页 DOM。新增同类栏目只需在 `internal/service/article/skip.go` 的前缀表中追加。

正文提取要求：标题、作者、正文段落纯文本；不抓取图片；图注保留为文本（题图 `dl.media_pic > dd`、正文图 `cximg > .article_img_talk`）；剔除页脚、推荐位、广告、相关阅读与付费墙节点，以及页面内植入的 AI 提示文本（`p.aitt`）和分页锚点（`a[name^="page"]`、`anchor[id^="page"]`）。

### 3.8 EPUB 组装

- 目录名：从 issue 页解析期号（如"财新周刊第1234期"）；解析失败回退 URL slug，规则为取 URL 中的年/期路径段拼连，如 `weekly.caixin.com/2026/cw1224/` → `2026-cw1224`。
- 期号/目录名做 sanitize：替换 `/`、`:` 等文件系统非法字符。
- EPUB 元数据：EPUB 3，语言 `zh-CN`，书名 = 期号，作者 = 财新周刊，章节按当期目录顺序、每章含作者署名。纯文字，无图片。
- 输出结构（`--out` 为父目录）：

```
<out>/财新周刊第1234期/
  ├── 财新周刊第1234期.epub
  ├── 财新周刊第1234期.mobi
  ├── articles/           # 中间产物：每篇纯文本
  └── .caixin2kindle/
      └── state.json      # 抓取状态
```

### 3.9 calibre 转换与 Kindle 拷贝

```
ebook-convert <期号>.epub <期号>.mobi --output-profile kindle
```

- 转换成功后才检测 Kindle——**程序启动时不做设备检测**，检测仅发生在需要拷贝这一步（未指定 `--no-kindle` 且 MOBI 已生成时）。检测规则：
  1. `--kindle` 指定的挂载点存在 → 使用之；
  2. 否则扫描 `/Volumes/*`，取第一个含 `documents/` 目录的卷；
  3. 均无 → 未检测到。
- 检测到 Kindle：将 MOBI 复制到其 `documents/` 目录；**同名文件已存在则覆盖**（与 `--full` 重建语义一致）；复制失败明确报错、退出码非 0。
- 未检测到 Kindle：提示"未检测到设备，成品文件位于 <输出目录>，请自行拷入"，**正常退出（退出码 0），不算失败**。
- `--no-kindle` 时跳过检测与拷贝。

---

## 4. 可靠性

### 4.1 增量（缺省行为）与状态文件

以 `<输出目录>/.caixin2kindle/state.json` 为唯一进度依据，**不靠数文章数**：

- `state.json` 记录期号，以及每篇文章的 URL、标题、状态（success/fail/pending）、纯文本文件路径、内容哈希。
- **内容哈希对提取后的正文纯文本计算**，不对原始 HTML（原始 HTML 含时间戳、推荐位等动态注入内容，会导致哈希误变、增量失效）。
- 重跑时：状态为 success 且内容哈希未变的文章跳过，其余补抓。
- **增量语义边界**：此处的"哈希未变"指**本地正文文件与 state 记录一致**，不检测财新网页是否更新（重跑不重新抓取、不联网比对）。若怀疑线上内容有更新，用 `--full` 重抓。该判定**只证明本地文件未被改动**，不代表线上未变。
- **期号隔离**：输出子目录按期号命名，同一 `--out` 先后运行不同期各自生成独立子目录与 state，互不干扰，无需冲突处理。
- EPUB/MOBI 重建判定：state 中全部文章 success **且**期号未变 → 跳过重建；否则重建。

### 4.2 `--full` 全量重跑

用户发现内容有问题需要彻底重抓时使用：忽略/重置既有 state，重新抓取当期全部文章并重建 EPUB/MOBI。

### 4.3 失败重试

- 单篇文章失败最多重试 2 次，间隔 2 秒、4 秒（固定退避）。
- **连续 3 篇**文章失败即停止运行，报错附排查提示：登录态是否失效 / 网络是否正常 / 页面结构是否变更。
- **单篇最终仍失败时的降级（2026-09-25 需求方确认）**：本期继续，用成功篇目构建 EPUB/MOBI，把未抓到的篇目在 stderr 记警告并在 stdout 列出清单（如“未抓取（1 篇）：第 21 篇 …”），退出码仍为 0。此时期望“抓取数 = 列表数”的验收标准 1/2 不成立，须以清单为准；若全部篇目都不可用，则按抓取失败退出码 3 处理，不产出空书。

### 4.4 登录态失效（中途）

运行中检测到被登出时暂停，按 3.2 的信号机制等待用户重新登录后继续。判定信号：文章页的 `div#chargeWallContent` 变为可见，或页面跳到登录页、出现验证码。

### 4.5 退出码与日志

| 退出码 | 含义 |
|---|---|
| 0 | 成功（含未检测到 Kindle 而提示自行拷入的情形） |
| 1 | 依赖缺失（Chrome / ebook-convert）或浏览器启动失败（含 profile 被占用） |
| 2 | 登录失败或登录超时 |
| 3 | 抓取失败（含连续 3 篇失败停止、单篇点击超限） |
| 4 | EPUB/MOBI 转换失败 |
| 5 | Kindle 复制失败 |

- 进度日志逐篇输出，格式：`第 3/32 篇：<标题>`。

---

## 5. 非目标（v1 不做）

- 图片抓取与嵌入
- 配置文件、环境变量、日志级别开关
- 多期批量、增量订阅
- Send-to-Kindle 云端推送
- Windows/Linux 支持
- 直接调用财新内部 API 抓取

---

## 6. 待验证事项

- [x] **收集三类样例页面 HTML 快照至 `testdata/`**（issue 页 / "余下全文"文章 / "下一页"文章），作为 selector 开发前置——2026-09-25 完成（第 1224 期 + 其封面报道 1 篇，另采未登录态 1 张）
- [x] 依快照确认 3.7 两种机制的实际行为（"余下全文"是否单击即完整、并存时的优先级）——与 3.7 描述一致（该篇共 4 个分页）
- [x] 依快照确定列表、标题、作者、正文、按钮的具体锚点（写入集中 selector 定义）——见 `architecture.md` §8.1 定稿表
- [x] 依快照确认文末特征、付费提示与期号文案——**"文末特征符号"实测不存在，3.7-7 已改为三条 DOM 事实**；付费判据为 `div#chargeWallContent` 是否可见；期号同时存在 `《财新周刊》第1224期` 与 `总第1224期` 两套写法
- [ ] 老款 Kindle 具体型号对 MOBI 子格式（KF7/KF8）的兼容性，必要时调整 `--output-profile`

---

## 7. 验收标准

1. 抓取文章数 = issue 页列表去重后的文章数（**有意跳过的图片型栏目除外**，见 3.7-8：这些篇目计入 `skipped` 并在输出中列出）；
2. 每篇正文完整（满足 3.7-7 的三条判据：无付费墙、按钮穷尽、小节齐全）且无付费提示残留；
3. EPUB 可正常打开（calibre 导入验证），元数据与章节正确；
4. `ebook-convert` 成功产出 MOBI；
5. 检测到 Kindle 时拷贝成功；未检测到时输出路径提示且退出码为 0；
6. 重跑同一期：已成功文章不重复抓取（增量生效）；
7. `--full` 后文章全部重新抓取并重建 EPUB/MOBI。
